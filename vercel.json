{
    "version": 2,
    "builds": [
        {
            "src": "index.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        { "src": "/doc/(.*)", "dest": "/doc/$1" },
        { "src": "/swagger/(.*)", "dest": "/swagger/$1" }
    ]
    
    
}